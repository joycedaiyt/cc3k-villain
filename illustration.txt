About rendering graphics

cell {
    char type: {wall, passage, empty,  ...}
}

when rendering, check for every character/items/
if checked character/item, we get the location
and we assign correct symbol to the corresponding cell

floor {
    vector<vector<cell>> cells;
    string map = " --- this is our empty map string --- ";
    vector<string> = map.split("\n");
    for (int i = 0; i < number_of_row; ++i) {
        for (int j = 0; j < number_of_column; ++j) {
            Cell new_cell = ...;
            floor->add_cell(new_cell)
        }
    }
}